FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && \
    apt-get install -y libreoffice libreoffice-calc fonts-nanum fonts-dejavu-core && \
    apt-get clean

WORKDIR /workspace
